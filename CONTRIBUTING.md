# CONTRIBUTING.md
# دليل المساهمة في تاج الإتقان 🙏

شكراً لاهتمامك بالمساهمة في مشروع تاج الإتقان! 

## قواعد المساهمة

### 1. التقرير عن الأخطاء 🐛

إذا وجدت خطأ، يرجى:
- التحقق من أنه لم يتم الإبلاغ عنه مسبقاً
- فتح issue جديد مع:
  - وصف واضح للمشكلة
  - خطوات إعادة الإنتاج
  - النتيجة المتوقعة والفعلية
  - معلومات النظام (OS، Python version)

### 2. تقديم ميزات جديدة 💡

قبل بدء العمل:
1. افتح issue للنقاش حول الفكرة
2. انتظر الموافقة من المشرفين
3. ابدأ العمل على فرع منفصل

### 3. معايير الكود 📝

#### أسلوب الكود
- اتبع [PEP 8](https://www.python.org/dev/peps/pep-0008/)
- استخدم أسماء واضحة وذات معنى
- أضف تعليقات للأجزاء المعقدة
- اكتب docstrings لجميع الدوال

#### مثال:
```python
def calculate_student_points(student_id: int, bonus: int = 0) -> int:
    """
    حساب نقاط الطالب الكلية
    
    Args:
        student_id: معرف الطالب الفريد
        bonus: نقاط إضافية (اختياري)
    
    Returns:
        إجمالي النقاط
    
    Raises:
        ValueError: إذا كان student_id غير صحيح
    """
    if student_id <= 0:
        raise ValueError("معرف الطالب يجب أن يكون موجباً")
    
    # الحساب هنا
    return points
```

### 4. الاختبارات ✅

- اكتب اختبارات لأي كود جديد
- تأكد من عدم كسر الاختبارات الموجودة
- استهدف نسبة تغطية 80% على الأقل

### 5. التوثيق 📚

- حدّث README.md إذا لزم الأمر
- اكتب تعليقات واضحة في الكود
- أضف أمثلة استخدام للميزات الجديدة

## عملية الإرسال (Pull Request)

### الخطوات:

1. **انسخ المستودع**
   ```bash
   git clone https://github.com/yourusername/taj-al-itqan.git
   cd taj-al-itqan
   ```

2. **أنشئ فرع ميزة**
   ```bash
   git checkout -b feature/description
   # أو للإصلاحات
   git checkout -b fix/description
   ```

3. **قم بالتغييرات والاختبارات**
   ```bash
   # قم بالتعديلات
   # اختبر الكود
   python -m pytest
   ```

4. **قم بالـ Commit**
   ```bash
   git add .
   git commit -m "وصف واضح للتغيير"
   ```

5. **ادفع التغييرات**
   ```bash
   git push origin feature/description
   ```

6. **افتح Pull Request**
   - اذهب إلى GitHub وافتح PR
   - اكمل نموذج PR
   - انتظر المراجعة

### نموذج رسالة Commit:
```
[TYPE] وصف قصير

وصف مفصل إذا لزم الأمر:
- النقطة الأولى
- النقطة الثانية

الإصلاحات: #issue_number
```

### أنواع Commit:
- `feat:` ميزة جديدة
- `fix:` إصلاح خطأ
- `docs:` تحديثات التوثيق
- `style:` تنسيق الكود (بدون تغيير منطق)
- `refactor:` إعادة هيكلة
- `test:` إضافة/تحديث الاختبارات
- `chore:` تحديثات أخرى

## معايير القبول

سيتم فحص PR من حيث:
- ✅ هل يتبع معايير الكود؟
- ✅ هل الاختبارات تمر؟
- ✅ هل التوثيق كافية؟
- ✅ هل هناك حالات حدية تم التعامل معها؟
- ✅ هل هناك تأثير سلبي على الأداء؟

## أسئلة شائعة

**س: ماذا لو لم أنتهِ من العمل؟**
- ضع `[WIP]` في عنوان PR (Work In Progress)

**س: كم من الوقت يستغرق فحص PR؟**
- عادة 1-2 أسبوع

**س: هل يمكنني العمل على ميزات متعددة؟**
- نعم، لكن في فروع منفصلة

**س: ماذا لو كان هناك تعارض؟**
- سيُطلب منك دمج التغييرات الأخيرة

## قواعس السلوك

- كن محترماً وودياً
- تقبل النقد البناء
- ركز على الفكرة وليس الشخص
- لا تتسامح مع المضايقات

## المساهمون

شكراً لكل من ساهم في هذا المشروع! 👏

---

**آخر تحديث**: 28 يناير 2026

للأسئلة: [فتح Issue](https://github.com/yourusername/taj-al-itqan/issues/new)
